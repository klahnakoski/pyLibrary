# encoding: utf-8
# THIS FILE IS AUTOGENERATED!
from __future__ import unicode_literals
from setuptools import setup
setup(
    description=str(u'Library of Wonderful Things'),
    license=str(u'MPL 2.0'),
    author=str(u'Kyle Lahnakoski'),
    author_email=str(u'kyle@lahnakoski.com'),
    long_description_content_type=str(u'text/markdown'),
    include_package_data=True,
    classifiers=["Development Status :: 4 - Beta","Topic :: Software Development :: Libraries","Topic :: Software Development :: Libraries :: Python Modules","License :: OSI Approved :: Mozilla Public License 2.0 (MPL 2.0)"],
    install_requires=["beautifulsoup4","boto","pymysql","requests"],
    version=str(u'2.54.19240'),
    url=str(u'https://github.com/klahnakoski/pyLibrary'),
    zip_safe=False,
    packages=["pyLibrary/aws","pyLibrary/env","pyLibrary/sql","pyLibrary/testing","pyLibrary/vendor","pyLibrary"],
    long_description={"$concat":["# pyLibrary",null,"A library of wonderful Python things!",null,"## Motivation",null,"This library is born from my version of the `utils` library every project has.","Only, instead of being utilities that are specific to the task, these utilities","are for multiple projects: They assume logs should be structured,","all data should be JSONizable, and OO is preferred, and more.",null,"### Python is a Little Crufty ###",null,"Python is awesome now, but it was originally a procedural language invented","before pure functional semantics, before OO, and even before the","discovery of vowels.  As a consequence there are many procedures that alter","their own parameters, or have disemvoweled names.  This library puts a facade","over these relics of the past and uses convention to name methods.",null,"## Installing pyLibrary",null,"Python packages are easy to install, assuming you have Python (see below).",null,"    pip install pyLibrary",null,"## Installing for Development",null,"  * Download from Github:",null,"        git clone https://github.com/klahnakoski/pyLibrary.git",null,"  * Install requirements:",null,"        python setup.py develop",null,null,"Windows 7 Install Instructions for Python","-----------------------------------------",null,"Updated November 2014, for Python 2.7.8",null,"Python was really made for Linux, and installation will be easier there.","Technically, Python works on Windows too, but there are a few gotchas you can","avoid by following these instructions.",null,"  * Download Python 2.7","    * 32bit ONLY!!! Many native libs are 32 bit","    * Varsion 2.7.8 or higher (includes pip, so install is easier)","  * Install Python at ```c:\\Python27``` (The space in the \"Program Files\" may screw up installs of native libs)","  * Add to you path: ```c:\\Python27;c:\\Python27\\scripts;```","  * Download ```https://bootstrap.pypa.io/get-pip.py```",null,"        CALL python get-pip.py","        CALL pip install virtualenv",null,"  * Many \"Python Powered\" native installs require a pointer to the python installation, but they have no idea where to","  look in 64bit windows.  You must alter the registry ([http://stackoverflow.com/questions/3652625/installing-setuptools-on-64-bit-windows](http://stackoverflow.com/questions/3652625/installing-setuptools-on-64-bit-windows)):",null,"        SET HKEY_LOCAL_MACHINE\\SOFTWARE\\Wow6432Node\\Python\\PythonCore\\2.7\\InstallPath = \"C:\\Python27\"",null,"###Using virtualenv",null,"```virtualenv``` allows you to have multiple python projects on the same","machine, even if they use different versions of the same libraries.","```virtualenv``` does this by making a copy of the main python directory and","using it to hold the specific versions required.",null,"* New environment: ```virtualenv <name_of_dir>```","* Activate environment: ```<name_of_dir>\\scripts\\activate```","* Exit environment: ```deactivate```",null,"If you have more than one project on your dev box I suggest you do all your","work inside a virtual environment.",null,"### PyPy and Virtual Environments",null,"```virtualenv``` can be used with PyPy, but it is a bit more involved.  The","paths must be explict, and some copying is required.",null,"#### New environment:","The first call to virtualenv will make the directory, to which you copy the","PyPy core libraries, and the second call finishes the install.",null,"    c:\\PyPy27\\bin\\virtualenv <name_of_dir>","    copy c:\\PyPy27\\bin\\lib_pypy <name_of_dir>","    copy c:\\PyPy27\\bin\\lib_python <name_of_dir>","    c:\\PyPy27\\bin\\virtualenv <name_of_dir>",null,"#### Activate environment:","With CPython ```virtualenv``` places it's executables in ```Scripts```.  The","PyPy version uses ```bin```",null,"    <name_of_dir>\\bin\\activate",null,"#### Using PIP in PyPy:",null,"PyPy does not share any libraries with CPython.  You must install the PyPy libraries using ",null,"\tC:\\pypy\\bin\\pip.exe",null,"The `pip` found in your `%PATH%` probably points to `C:\\python27\\Scripts\\pip.exe`.",null,"#### Using PIP in PyPy virtualenv:",null,"Do **NOT** use the ```<name_of_dir>\\Scripts``` directory: It installs to your","main PyPy installation.  Pip install is done using the `bin` directory:",null,"    <name_of_dir>\\bin\\pip.exe",null,"#### Exit environment:","Deactivation is like normal",null,"    deactivate",null,"### CPython Binaries and Virtual Environments",null,"If you plan to use any binary packages, ```virtualenv``` will not work","directly.  Instead, install the binary (32 bit only!!) to the main python","installation.  Then copy any newly installed files/directories from","```C:\\Python27\\Lib\\site-packages``` to ```<name_of_dir>\\Lib\\site-packages```.",null,"### Binaries and PyPy",null,"This strategy for installing binaries into Virtual Environments is almost","identical to installing binaries into your PyPy environment: Install Numpy","and Scipy to your CPython installation using a windows installer (which has","pre-compiled binaries), and then copy the ```C:\\Python27\\Lib\\site-packages\\<package>```","to ```c:\\PyPy\\site-packages\\```; note lack of ```Lib``` subdirectory.",null,null]},
    name=str(u'pyLibrary')
)